<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="item"
            type="thecyberseguy.usersapp.models.User" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <androidx.cardview.widget.CardView
            android:id="@+id/card_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/standard"
            android:layout_marginTop="@dimen/standard"
            app:cardCornerRadius="@dimen/small"
            app:cardElevation="2dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_item_user"
                android:orientation="vertical"
                android:padding="@dimen/standard">

                <TextView
                    android:id="@+id/tv_user_name"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@{item.name}"
                    android:textColor="@color/black"
                    android:textSize="@dimen/large_text_1"
                    android:textStyle="bold"
                    tools:text="The Cyberse Guy" />

                <TextView
                    android:id="@+id/tv_user_email"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/tiny"
                    android:text="@{item.email.toLowerCase()}"
                    android:textColor="@color/black"
                    android:textSize="@dimen/large_text_1"
                    tools:text="thecyberseguy@gmail.com" />

            </LinearLayout>

        </androidx.cardview.widget.CardView>

        <Space
            android:id="@+id/bottom_space"
            android:layout_width="match_parent"
            android:layout_height="@dimen/standard"
            android:visibility="gone"
            tools:visibility="visible" />

    </LinearLayout>

</layout>